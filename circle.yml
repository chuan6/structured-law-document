general:
  build_dir: generator

test:
  override:
    - lein test generator.core generator.tokenizer generator.line generator.lisp generator.zh-digits generator.test generator.parse-tree generator.id
